# Runtime
pymupdf>=1.24        # PDF text, font and table extraction
lxml>=5.0            # XML building and XSD validation

# Web UI (webui/) -- viewing generated documents, not part of the pipeline
fastapi>=0.115
uvicorn>=0.30
jinja2>=3.1
python-multipart>=0.0.9   # form parsing for the /convert endpoint

# Development
pytest>=8.0
